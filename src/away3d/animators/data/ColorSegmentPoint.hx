package away3d.animators.data;

extern class ColorSegmentPoint {
	var color(default,never) : flash.geom.ColorTransform;
	var life(default,never) : Float;
	function new(p1 : Float, p2 : flash.geom.ColorTransform) : Void;
}
